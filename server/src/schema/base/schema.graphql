"""
A normalized object that can be addressed by its globally unique ID.
"""
interface Entity {
  id: ID!
}

interface DeletedEntity {
  id: ID!
}

scalar NonEmptyString

scalar Slug

scalar DateTimeISO
